

mkdir build
cd build

#Build with

C=/usr/bin/clang CXX=/usr/bin/clang++ cmake -DLLVM_DIR=$LLVM_INSTALL_DIR/share/llvm/cmake ../optdetect/

# or

C=gcc CXX=g++ cmake -DLLVM_DIR=$LLVM_INSTALL_DIR/share/llvm/cmake ../optdetect

